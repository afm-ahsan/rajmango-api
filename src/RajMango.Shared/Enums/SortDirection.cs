namespace RajMango.Shared.Enums
{
    public enum SortDirection
    {
        None = 0,        // No sort direction selected

        Ascending = 1,   // A → Z, 1 → 9
        Descending = 2   // Z → A, 9 → 1
    }
}
