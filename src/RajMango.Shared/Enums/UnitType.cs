namespace RajMango.Shared.Enums
{
    public enum UnitType
    {
        None = 0,     // Default/unselected

        Kg = 1,       // Kilogram
        Piece = 2,    // Individual item
        Box = 3,      // Pre-packed box
        Crate = 4     // Bulk crate (often 10kg or 20kg)
    }
}
